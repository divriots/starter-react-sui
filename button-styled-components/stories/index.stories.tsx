import React from 'react';
import { Button } from '../index';
import { decorators } from '~/story-decorator-styled-components';
export default { decorators }

export const buttons = () => <Button>Press me</Button>;
